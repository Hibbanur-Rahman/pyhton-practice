# C part a
a=12.25
b=12.52
if a==b:#the problem is that in question a=b which is asssignment operator and not the conditional operator ehich causes error
    print("a and b are equal")

#C part b
""" 
    if ord('X')<ord('x')
        print("unicode value of X is smaller than that of x")
"""
if ord('X')<ord('x'):
        print("unicode value of X is smaller than that of x")
#in this code the ':' is not given in the if function which is if terminator so the error is display


